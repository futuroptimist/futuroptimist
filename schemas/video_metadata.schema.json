{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "VideoMetadata",
  "type": "object",
  "required": ["youtube_id", "title"],
  "properties": {
    "youtube_id": {"type": "string"},
    "title": {"type": "string"},
    "publish_date": {"type": "string", "format": "date"},
    "duration_seconds": {"type": "integer", "minimum": 0},
    "keywords": {
      "type": "array",
      "items": {"type": "string"}
    },
    "status": {
      "type": "string",
      "enum": ["draft", "live", "private"]
    },
    "description": {"type": "string"},
    "slug": {"type": "string"},
    "thumbnail": {"type": "string"},
    "transcript_file": {"type": "string"},
    "summary": {"type": "string"},
    "view_count": {"type": "integer", "minimum": 0},
    "analytics": {
      "type": "object",
      "properties": {
        "views": {"type": "integer", "minimum": 0},
        "watch_time_minutes": {"type": "number", "minimum": 0},
        "average_view_duration_seconds": {"type": "number", "minimum": 0},
        "impressions": {"type": "integer", "minimum": 0},
        "impressions_click_through_rate": {"type": "number", "minimum": 0},
        "updated_at": {"type": "string", "format": "date-time"}
      }
    }
  }
}
